<p id="notice"><%# notice %></p>

<%# <h1>Phones Nearby You</h1> %>

<table class='pure-table' style='width: 100%'>
  <thead>
    <tr>
      <th class='pure-table-odd'><%= t('index_ph') %></th>
      <%# <th>La</th>
      <th>Lo</th>
      <th>Ip</th>
      <th colspan="3"></th> %>
    </tr>
  </thead>

  <tbody>
    <tr class='pure-table-odd'>
    </tr>
    <% @calls.each do |call| %>
      <tr class='pure-table-odd'>
        <% if session[:current_tel] != call.tel %>
          <td><a href="tel:<%= call.tel %>"><font size=5>[<%= t call.style %>] <%= call.action !='' ? call.action : call.tel %></a><a href="https://www.google.com/maps/search/<%= call.action %>"> 📍</a> </font></td>
        <% end %>
      </tr>
    <% end %>
  </tbody>
</table>

<br>
<center>
<font size=5>
<% if @calls.count != 1 %>
<%= link_to '🖊️ ' + Call.find(session[:current_id]).action, :controller => 'calls', :action => 'edit', :id => session[:current_id] %>
|
<%= link_to '🔙 ' + t('index_end'), Call.find(session[:current_id]), method: :delete, data: { confirm: t('index_out_noti') } %> 
<% end %> 
</font>
</center>

<%# link_to 'New Call', new_call_path %>
